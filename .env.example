# Leave unset to run the deterministic offline stack (no network needed).
# OPENAI_API_KEY=sk-...
# Force offline even when a key is present.
OFFLINE_MODE=1

CHAT_MODEL=gpt-4o-mini
EMBED_MODEL=text-embedding-3-small

TOP_K=4
DENSE_WEIGHT=0.6
MIN_LEXICAL_SCORE=6.0
MIN_DENSE_SCORE=0.45
